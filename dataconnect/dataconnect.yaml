specVersion: "v1"
serviceId: "organization-calendar"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "organization-calendar-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
